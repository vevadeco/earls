{
  "name": "earls-landscaping",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r build ../",
    "dev": "cd frontend && npm start"
  }
}
